# Trilingual Gap Analysis

Updated: 2026-06-29  
Branch: `feature/de-articles-expansion`

## Status

**READY**

The strict public publication gap is closed.

## Matrix Snapshot

| Metric | IT | EN | DE |
| --- | ---: | ---: | ---: |
| Source corpus | 97 | 97 | 97 |
| Strict public perimeter | 97 | 97 | 97 |
| Sitemap entries | 97 | 97 | 97 |
| Public build entries | 97 | 97 | 97 |

## Gap Closure

### EN

The missing public EN equivalents were created and validated:

- `thematic-itineraries-montefiascone`
- `winter-in-montefiascone`
- `editorial-project-montefiascone`

The three EN outliers are no longer treated as matrix gaps. They remain source-only exceptions excluded from the strict public set:

- `how-many-days-in-montefiascone`
- `where-to-stay-around-lake-bolsena`
- `where-to-stay-montefiascone-vs-bolsena-viterbo`

### DE

All 97 IT public rows have a DE equivalent in the strict public perimeter.

The five evergreen DE blocker pages were lifted above `300` words, which removes the previous quality blocker for the public gate.

## DE Editorial Follow-Through

The post-gate German thin-content backlog has now been fully closed:

- the previously documented `16` DE short pages were strengthened
- a final follow-up sprint lifted the remaining `14` DE public pages still under `220` words
- all public DE pages in scope are now above the prior `220`-word risk threshold
- the public matrix remains unchanged at `97 / 97 / 97`

## Release Meaning

This document now separates two concepts cleanly:

- **publication gap:** closed
- **editorial improvement backlog:** the previously blocking public DE thin-content set is now closed

The project is therefore `READY` for the strict trilingual public perimeter and no longer carries the previously documented public DE thin-content blocker set.
